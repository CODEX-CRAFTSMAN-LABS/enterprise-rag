pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

plugins {
    id 'com.diffplug.spotless' version '6.25.0' apply false
}

rootProject.name = 'enterprise-rag'

include 'rag-common', 'ingestion-service', 'query-service'
