# Copy to jenkins/.env and adjust for Colima on macOS:
#   cp jenkins/.env.example jenkins/.env
#
# Colima Docker socket (typical):
# DOCKER_SOCK=${HOME}/.colima/default/docker.sock
#
# Kubeconfig (after: colima start --kubernetes)
# KUBE_CONFIG=${HOME}/.kube/config
